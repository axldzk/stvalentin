<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Saint Valentin 💖</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      background: #ffe6eb;
      padding-top: 40px;
    }

    h1 {
      color: #d6336c;
    }

    img {
      width: 220px;
      margin: 20px 0;
    }

    button {
      font-size: 20px;
      padding: 10px 22px;
      margin: 10px;
      cursor: pointer;
      border-radius: 8px;
      border: none;
      transition: transform 0.2s;
    }

    .yes {
      background-color: #ff4d6d;
      color: white;
    }

    .no {
      background-color: #adb5bd;
      color: white;
    }

    #step2 {
      display: none;
    }
  </style>
</head>
<body>

  <!-- ÉTAPE 1 -->
  <div id="step1">
    <h1>Veux-tu être ma Valentine ? 💘</h1>

    <img src="https://media.giphy.com/media/l0MYt5jPR6QX5pnqM/giphy.gif">

    <div>
      <button class="yes" id="yes1">OUI 💖</button>
      <button class="no" id="no1">NON 😢</button>
    </div>
  </div>

  <!-- ÉTAPE 2 -->
  <div id="step2">
    <h1>T’es sûr(e) de toi ?? 😳</h1>
    <h2>Réfléchis bien !!!</h2>

    <img src="https://media.giphy.com/media/3orieTfp1MeFLiBQR2/giphy.gif">

    <div>
      <button class="yes" id="yes2">OUIS 💖</button>
      <button class="no" id="no2">Euh… 😬</button>
    </div>
  </div>

  <script>
    let yesScale1 = 1;
    let noScale1 = 1;
    let yesScale2 = 1;
    let noScale2 = 1;

    // ÉTAPE 1
    document.getElementById("yes1").onclick = () => {
      document.getElementById("step1").style.display = "none";
      document.getElementById("step2").style.display = "block";
    };

    document.getElementById("no1").onclick = () => {
      noScale1 -= 0.2;
      document.getElementById("no1").style.transform = `scale(${Math.max(noScale1, 0.3)})`;
    };

    // ÉTAPE 2
    document.getElementById("yes2").onclick = () => {
      yesScale2 += 0.3;
      noScale2 -= 0.2;
      document.getElementById("yes2").style.transform = `scale(${yesScale2})`;
      document.getElementById("no2").style.transform = `scale(${Math.max(noScale2, 0.3)})`;
    };

    document.getElementById("no2").onclick = () => {
      noScale2 -= 0.2;
      document.getElementById("no2").style.transform = `scale(${Math.max(noScale2, 0.3)})`;
    };
  </script>

</body>
</html>
